apiVersion: v1
kind: Service
metadata:
  namespace: smoketest
  labels:
    app: smoketest-service
  name: smoketest-service
spec:
  ports:
  - name: 80-8080
    port: 80
    targetPort: 8080
  selector:
    app: smoketest-deployment
